[project]
name = "2025-ai-publication-analysis"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "openai>=1.0.0",
    "polars>=0.20.0",
    "pydantic>=2.0.0",
]

[dependency-groups]
dev = [
    "ipython>=9.10.0",
]
